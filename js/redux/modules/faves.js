// import { formatSessionData } from '../../lib/dataFormatHelper';
// import { getFaves } from '../../config/models';
// import { formatSessionData } from '../../lib/dataFormatHelpers';

// const FAVES_DATA = "FAVES_DATA";

// export const getFavesData = () => { dispatch } => {
//   dispatch(getFaves());
//   return fetch( `${firebaseUrl}/sessions.json`)
//   .then(response => response.json())
//   .then(data => data.filter( session =>  {
//     if(faveIds.includes(session.session_id)) {
//       return session;
//     }
//   }))
// }